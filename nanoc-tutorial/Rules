#!/usr/bin/env ruby

compile "/users/*.html" do
  layout "/user.*"
  write item.identifier.without_ext + ".html"
end

compile "/**/*.scss" do
  filter :sass, syntax: :scss
  write item.identifier.without_ext + ".css"
end

layout "/**/*", :erb
